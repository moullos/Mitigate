name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: EasyDesk/action-dotnet-build@v1
      with:
        build-configuration: Release
